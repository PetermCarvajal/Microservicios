server:
  port: 8081

spring:
  application:
    name: taxonomia-service

  datasource:
    url: jdbc:mysql://localhost:3306/taxonomia_microservicio?useSSL=false&serverTimezone=UTC
    username: root
    password: qwertyuiop
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect