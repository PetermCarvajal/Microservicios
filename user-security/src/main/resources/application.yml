server:
  port: 8085

spring:
  application:
    name: producto-service

  datasource:
    url: jdbc:mysql://localhost:3306/user_microservicio?useSSL=false&serverTimezone=UTC
    username: root
    password: qwertyuiop
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect